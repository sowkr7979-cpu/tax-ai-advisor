"""src/ — deterministic engine (docs/README §2). Ground-truth calc, RAG
orchestration, isolation, citation verification, conflict detection, audit, DOCX.

NO vendor SDK calls here — those live in src/ai/ (adapters).
"""
