"""tiw — Tax Intelligence Workspace orchestration + evaluation harness package.

The deterministic engine (src/), contracts (contract/) and rules (rules/) live at
the Workpaper-2120 repo root; tiw/ holds the eval harness (tiw.eval) and will hold
orchestration wiring as slices land.
"""

__version__ = "0.1.0"
