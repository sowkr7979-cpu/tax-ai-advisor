# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Secrets / env (NEVER commit) ---
.env
.env.*
!.env.example
*.key
*.pem
infra/config/secrets*
**/secrets.*

# --- Runtime data (README boundary: 런타임 데이터 ≠ 소스코드) ---
runs/
logs/
*.log
# vector DB / embeddings store (data, not source)
.chroma/
*.duckdb
*.sqlite
*.sqlite3
# 내부 RAG DB — 원본 PDF(공개 세무 가이드, 287MB)와 임베딩 인덱스는 런타임 데이터.
# 코드·매니페스트·README 만 형상관리(코퍼스는 각자 내려받아 scripts/build_rag_db_index.py 로 재생성).
RAG DB/*.pdf
RAG DB/_index/

# DART corp_code 매핑 캐시(12MB 대용량 — 키로 재생성 가능). 회사 재무 fixture(소형)는 커밋.
tests/fixtures/dart/corp_code_map.json

# NOTE: tests/golden/ and tests/fixtures/ ARE committed (ground truth + deterministic replay)

# --- Node (frontend mockup) ---
node_modules/
.next/
.vite/
dist-frontend/
frontend/dist/
frontend/test-results/
frontend/playwright-report/
frontend/screenshots/
frontend/.playwright/
.playwright-cache/

# --- generated demo docx (regenerable) ---
*.docx
!docs/**/*.docx
!Codex/**/*.docx

# --- 소개/시연 빌드 산출물 (regenerable) ---
# (주석은 반드시 별도 줄에 — .gitignore는 인라인 주석 미지원)
# KICPA PDF 임베드용 다운샘플·도식 캐시
project_intro_assets/
big4_learning_assets/
big4_learning_pdf_render/
docs_kicpa_visual_assets/
# 루트 PDF는 뷰어 잠금 시 구버전이 남을 수 있어 형상관리 제외(웹 사본 web/assets/ 만 커밋)
/세무AI_프로젝트소개_KICPA용.pdf
/법인세세무조정자동화_프로젝트소개_KICPA용.pdf
# 개인 자기소개서 PDF(민감·재생성) — 스크립트는 커밋, 산출 PDF/임시파일은 제외
/자기소개서_안진회계법인_Tax부문.pdf
/자기소개서_안진회계법인_Tax부문.tmp.pdf
# 산출물(생성 DOCX·차트 — 스크립트로 재생성, 대용량이라 형상관리 제외)
산출물/
산출물/_charts*/
# 웹 데모 데이터: 브라우저는 .js만 로드. .json은 중복(export로 재생성) → 제외
web/data/scenarios.json
# 라이브 백엔드 세션 산출물(런타임 — 재생성)
web/backend/_reports/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# TS incremental build cache
*.tsbuildinfo

.vercel

# --- 개인 자료 (공개 저장소 제외 — 자소서·타사 자료·개인 보고서) ---
타사/
자기소개서/
*자기소개서*
scripts/build_coverletter_pdf.py
scripts/build_ey_coverletter*.py
scripts/build_kpmg_coverletter*.py
한미반도체*.pdf
# 면접·이직 준비 개인 자료 (공개 저장소 제외)
md 파일/*면접*
md 파일/Big4_Tax_*
md 파일/법인세_세무AI_설계서_통합.md
md 파일/법인세_세무AI_설계서_통합.html
